require 'spec_helper'

describe Wechat::Core do
  it 'has a version number' do
    expect(Wechat::Core::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
